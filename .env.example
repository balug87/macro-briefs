# Required. No fallback: blank means /api/auth/* returns 500 MissingSecret.
AUTH_SECRET=
AUTH_GITHUB_ID=
AUTH_GITHUB_SECRET=
AUTH_URL=https://YOUR-PROJECT.vercel.app
GITHUB_ALLOWLIST=balug87
# 1 makes every brief public. Anything else gates them. Production runs 0.
PUBLIC_PREVIEW=1
